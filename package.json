{
  "name": "ziazon-profile",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "node --test \"scripts/**/*.test.mjs\"",
    "generate": "node scripts/generate-stats.mjs"
  }
}
